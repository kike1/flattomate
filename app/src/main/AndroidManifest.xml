<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="myapp.myapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- MAIN ACTIVITY -->
        <activity
            android:name="myapp.flattomate.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LOGIN ACTIVITY -->
        <activity
            android:name="myapp.flattomate.LoginActivity"
            android:label="@string/title_activity_login"
            android:parentActivityName="myapp.flattomate.HomeActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="myapp.flattomate.HomeActivity" />
        </activity>

        <!-- REGISTER ACTIVITY -->
        <activity
            android:name="myapp.flattomate.RegisterActivity"
            android:label="@string/title_activity_activity_signin"
            android:parentActivityName="myapp.flattomate.HomeActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="myapp.flattomate.HomeActivity" />
        </activity>

        <!-- DASHBOARD ACTIVITY -->
        <activity android:name="myapp.flattomate.DashboardActivity" />

        <!-- EDIT_PROFILE ACTIVITY -->
        <activity
            android:name="myapp.flattomate.EditProfileActivity"
            android:label="@string/profile_edit_title"
            android:parentActivityName="myapp.flattomate.ProfileActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="myapp.flattomate.ProfileActivity" />
        </activity>

        <!-- PROFILE ACTIVITY -->
        <activity
            android:name="myapp.flattomate.ProfileActivity"
            android:parentActivityName="myapp.flattomate.DashboardActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="myapp.flattomate.HomeActivity" />
        </activity>
        <activity android:name="myapp.flattomate.OwnProfileActivity" />
        <activity
            android:name="myapp.flattomate.ShowImageProfile"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_show_image_profile"
            android:parentActivityName="myapp.flattomate.OwnProfileActivity"
            android:theme="@style/FullscreenTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="myapp.flattomate.OwnProfileActivity" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>